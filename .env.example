# Optional: OpenAI API key for live LLM responses
# If not set, the app uses mock/fallback mode
OPENAI_API_KEY=

# LLM mode: "mock" (default) or "openai"
LLM_MODE=mock

# Database path (optional, defaults to ./data/datax.json)
DATABASE_PATH=./data/datax.json
